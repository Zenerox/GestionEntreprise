{% if app.user %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_index') }}">Gestion Ma Boîte</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a {% if navStock is defined %}
                                class="nav-link active" aria-current="page"
                            {% else %}
                                class="nav-link"
                            {% endif %}
                            href="{{ path('app_list_stock') }}">Gestion du stock
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        {#  <a {% if navClient is defined %}
                                class="nav-link active" aria-current="page"
                            {% else %}
                                class="nav-link"
                            {% endif %}
                            href="{{ path('app_list_client') }}">Gestion des clients
                        </a>#}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Gestion des clients
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="{{ path('app_list_client') }}">Liste des clients</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_create_client') }}">Ajouter un client</a></li>
                        </ul>
                    </li>
                    <li class="nav-item">
                        {#<a {% if navRdv is defined %}
                                class="nav-link active" aria-current="page"
                            {% else %}
                                class="nav-link"
                            {% endif %}
                            href="{{ path('app_list_rdv') }}">Gestion des rendez-vous
                        </a>#}
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Gestion des Rdv
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="{{ path('app_planning_rdv') }}">Planning</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_create_rdv') }}">Ajouter un rdv</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
            <div><a class="nav-logout" href="{{ path('app_logout') }}">Se déconnecter</a></div>
        </div>
    </nav>
{% endif %}